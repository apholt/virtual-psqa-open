fastapi>=0.111.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.30
alembic>=1.13.0
pydicom>=2.4.4
SimpleITK>=2.4.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-multipart>=0.0.9
watchdog>=4.0.1
numpy>=1.26.4
aiofiles>=23.2.1
httpx>=0.27.0
# Machine learning prediction engine (Phase 4)
scikit-learn>=1.5.0
pandas>=2.2.0
joblib>=1.4.0
# Report generation — gamma-map thumbnails (Phase 5)
Pillow>=10.3.0
matplotlib>=3.8.0
# Optional: true server-side PDF rendering. On Windows this also needs the GTK
# runtime (Pango/Cairo); if unavailable, reports are served as print-ready HTML.
weasyprint>=62.3 ; platform_system != "Windows"
